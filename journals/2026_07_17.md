- 17.21
	- Notes
		- We can get so far in life by finishing the loop. 
		  logseq.order-list-type:: number
		- In life, there is always half-baked conditions in everything, and each time we close the loop, we stand firm until the very end, it will make it so that we have one more step ahead towards accomplishing something beyond our current situation. 
		  logseq.order-list-type:: number
		- So, stay firm and continue closing the loop of activities that we had, whatever it was. 
		  logseq.order-list-type:: number
		- Whether it was something small or even smaller than that, finish and complete it. 
		  logseq.order-list-type:: number
		- The priority was to complete, and not to perfect, so DO IT EITHER WAY!
		  logseq.order-list-type:: number
- 18.35
	- Finished
		- Completely connect logseq memo with git, will try pull and push it.
		  logseq.order-list-type:: number
	- To do
		- Make a log, in the shortcut, so that each time changes happen it is being stored? 
		  logseq.order-list-type:: number
		- I think number 1 will make the directory bloated, will not continue and let it be because git already handle the changes.
		  logseq.order-list-type:: number
- 18.38
	- Finished
		- Completely had a robust workflow for logseq synchronization from termux-github, and already utilize shortcut for pull-push in termux widget. Each changes made in logseq can easily be saved and pushed into github, so that it synchronized easily for each system. 
		  logseq.order-list-type:: number
- 18.40
	- Notes
		- Current situation make is so that this small step can make us move onward. 
		  logseq.order-list-type:: number
		- Utilization of logseq, which have complex and complicated data storing, is one of the main point of having synced knowledge-transfer. With this, further progress can be tracked, and we won't be easily lost with ourselves. Hell yeah. 
		  logseq.order-list-type:: number
		- Key things to note is that, to connect, utilize ssh from each device.
		  logseq.order-list-type:: number
- 19.38
	- To do
		- Will try and make interactive commit message when git push.
		  logseq.order-list-type:: number
- 19.46
	- Finished
		- Tried the first option, where it runs with widget, it works.
		  logseq.order-list-type:: number
		- Modified so that user can add commit message, it works, and add placeholder if user does not input message. If user input message, placeholder will not be included. 
		  logseq.order-list-type:: number
		- Make it so that placeholder was included regardless user input / not. If user input, placeholder positions first, then user input message.
		  logseq.order-list-type:: number
- 19.50
	- Notes
		- This current situation improve possibilities further. Meaning, we can propose QoL where each time we make changes in termux, we can push into git. By utilizing dynamicity of widgets, it's possible. 
		  logseq.order-list-type:: number
- 20.02
	- Finished
		- The dynamic works. Meaning, user can input the name of said folder, and await for the widgets finish iterating pull and push of that into respected github.
		  logseq.order-list-type:: number
-
-
-
-